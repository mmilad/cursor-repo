# Managed slot template (copy into `projects/<slug>/`)

This folder is **tracked** in the wrapper repo as a reference only. Copy its files into an **ignored** path such as `projects/acme-api/` after you create that directory locally.

See [`docs/managed-projects.md`](../../managed-projects.md).
