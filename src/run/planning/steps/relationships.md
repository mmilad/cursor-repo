Focus on **structure**: how pieces depend on each other.

- Draw the dependency / ordering story (what must happen before what).
- Flag **external contracts** (APIs, events, shared libraries, env vars).
- Surface **risky couplings** (tight loops, shared mutable state, implicit assumptions).

Build on the prior needs and affected-area notes.
